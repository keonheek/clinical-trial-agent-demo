# "추천"이 뭘 의미하는지 정의가 필요한 이유 — 백엔드 팀 공유용

## 왜 이게 "정렬 함수 하나"의 문제가 아닌가

지금 추천 순위는 사실상 이 규칙 하나로 결정됩니다.

```
같은 eligibility 상태(ELIGIBLE/UNCERTAIN/INELIGIBLE) 안에서는
미해결(UNCERTAIN/UNKNOWN) criterion이 적은 시험이 우선
```

이건 "아직 정의를 안 한 것"이 아니라, **이미 하나의 정의를 암묵적으로 선택해서 배포한 것**입니다.
그 정의는: "적합도 = criterion을 얼마나 많이/적게 확인해야 하는가".

문제는 이 정의가 다음 두 가지를 구분하지 못한다는 것입니다.

- 미해결이 적은 이유가 **"환자 정보가 실제로 충분해서"** 인 경우
- 미해결이 적은 이유가 **"parser가 애초에 그 시험의 원문을 덜 읽어서"** 인 경우

두 경우 모두 시스템은 겉보기에 "더 확실한 시험"이라고 보여주지만, 후자는 사실 **덜 검토된 시험**입니다.

---

## 우리 정답지에서 실제로 나오는 반증 사례

`eval_labels_stress.json`의 baseline 환자(T001~T005)만 봐도 criterion 개수가 이미 다릅니다.

| 시험 | 파싱된 criterion 수 | 미해결 수 |
|---|---|---|
| T001 (종양학, PEVRAD) | 4개 | 1개 (organ function 정의 없음) |
| T002 (심혈관, HFrEF) | 3개 | 0개 |
| T003 (신경퇴행, 알츠하이머) | 2개 | 0개 |
| T004 (자가면역, RA) | 3개 | 0개 |
| T005 (희귀질환, RP) | 3개 | 0개 |

지금 로직대로면 **T003(신경퇴행)이 T001(종양학)보다 "더 확실한 시험"으로 상위 추천됩니다** — 미해결이 0개니까요.

하지만 실제로는:
- T001은 원문 eligibility가 30개 조항(inclusion 7 + exclusion 30)이나 되는 시험이고, 그중 4개만 정답지에 뽑혀 들어간 것입니다.
- T003은 원문이 훨씬 짧은 시험이라 2개만 뽑혀도 원문을 거의 다 커버합니다.

즉 T003이 T001보다 "확인할 게 적어서 더 확실한 시험"이 아니라, **원문 자체가 짧아서 갯수가 적게 나온 것뿐**입니다. 지금 로직은 이 둘을 구분하지 못합니다.

### 더 위험한 버전 (parser가 실제로 덜 읽는 경우)

만약 매처가 T001의 30개 조항 중 4개만 뽑고, 그중 하나(organ function)가 하필 "구체적 수치 정의가 없어 판정 불가"인 조항이라면:
- 시스템은 이걸 "1개 미해결"로만 보여줍니다.
- 하지만 파싱에서 빠진 나머지 26개 조항 중에 결정적 exclusion이 숨어있을 수 있습니다.
- 사용자는 "1개만 확인하면 되는 꽤 확실한 시험"이라고 오해하게 됩니다.

**"미해결 개수가 적다" = "parser가 원문을 많이 커버했는데 그중 대부분이 명확했다"일 수도, "parser가 원문을 조금만 읽었는데 그게 우연히 다 명확했다"일 수도 있습니다. 지금 시스템은 이 둘을 구분할 방법이 없습니다.**

---

## 그래서 "추천"을 뭐라고 정의할 것인가 — 선택지 표

| 정의 | 의미 | 장점 | 위험 |
|---|---|---|---|
| **A. 확인 부담 최소화** (현재 암묵적 기본값) | 미해결 criterion이 적은 시험 우선 | 구현 이미 되어 있음 | parser coverage가 안 보이면 "덜 읽은 시험"이 1등이 될 수 있음 (위 반증 사례) |
| **B. Eligibility 가능성 우선** | Hard FAIL이 없고 PASS 비율이 높은 시험 우선 | "떨어질 가능성 낮음"을 직접 반영 | 미해결 criterion의 위험도(치명적 vs 사소한)를 반영 못함 |
| **C. 질환/중재 관련성 우선** | trial_intent, condition match가 강한 시험 우선 | 임상적으로 가장 의미 있는 "적합도" | ~~전부 TREATMENT라 변별력 없음~~ → **실측으로 반박됨(2026-08-16).** `build_trial_intent.py`가 원문 필드(title·phase·conditions)로 재분류한 결과, 데모 40개 시험 = **관찰 28(그중 저확신 19) · 치료 11 · 의료전달 1**. 축은 실제로 갈린다. 남은 위험은 저확신 분류이므로, 저확신은 관찰과 동급으로 강등해서 쓴다 |
| **D. 커버리지 정규화 + 확인 부담** (권장) | (미해결 수 / 파싱된 전체 수)로 정규화하고, coverage가 낮으면 순위 자체를 보류 표시 | 반증 사례 해결, "덜 읽은 시험"이 부당하게 1등 못 함 | 구현 비용 있음 — coverage report 필드 필요 |

---

## 제안

1. **당장 순위 알고리즘을 D로 전면 교체하자는 게 아닙니다.** 이번 대회 규모에서는 A를 유지하되, **최소한 "이 시험은 원문 대비 몇 %가 커버됐는지"를 화면에 같이 보여주는 것**만으로도 반증 사례의 위험을 크게 줄일 수 있습니다.
2. 발표에서는 "우리는 A를 알고 쓴다"와 "몰라서 A만 있다"가 완전히 다른 인상을 줍니다. coverage 표시 하나로 "이 팀은 자기 시스템의 한계를 알고 설계했다"는 신호를 줄 수 있습니다.
3. 이건 코드 몇 줄 문제가 아니라 **"ELIGIBLE"이라는 단어가 사용자에게 뭘 약속하는지"의 문제**입니다 — parser가 원문의 13%만 읽고 낸 ELIGIBLE과 90%를 읽고 낸 ELIGIBLE은 신뢰도가 다른데, 지금 UI에는 이 차이가 전혀 안 보입니다.

---

## 결정 (2026-08-16)

이 문서가 제기한 문제는 닫혔다. 배포된 순위 키는 **A(정규화 형태) + C(중재 목적) + Phase**의 조합이고,
D는 부분 채택했다. 구현은 `ranking.py`(순수 모듈, LLM 임포트 없음), `RANKING_VERSION = "2026-08-16"`.

### 실행되는 정렬 키 (사전식 — 앞 단계가 항상 우선)

```
0. 적격성 클래스     ELIGIBLE < UNCERTAIN < INELIGIBLE      (decide_eligibility)
1. 차단 기준 수      FAIL 개수 적은 순
2. 중재 목적         therapeutic < supportive < care_delivery < observational
                     확신 낮음 / 미분류 → observational과 동급으로 강등
3. Phase 부담        PHASE4 < PHASE3 < PHASE2 < PHASE1 = EARLY_PHASE1, NA는 중간
4. 커버리지 감점      파싱수 / 원문추정 < 0.5 이면 감점
5. 미해결 비율       REVIEW / 파싱수, 오름차순          ← 이 문서의 D안(정규화)
6. 미해결 수         REVIEW 개수, 오름차순
7. nct_id            재현 가능한 최후 tie-break
```

- **A는 버린 게 아니라 정규화됐다.** 원래의 "미해결 개수" 축은 5·6번으로 살아 있되, 비율이 먼저 온다.
  이것이 이 문서가 권장한 D안의 핵심(정규화)이다.
- **D의 나머지 절반(보류 표시)은 감점으로 바꿨다.** "순위 보류"라는 별도 상태를 만드는 대신,
  커버리지 50% 미만이면 같은 조건의 완독 시험보다 아래로 내린다. 단, 이 감점은 `coverage_map.json`에
  원문 추정치가 있는 시험에만 걸린다 — 데모 40개 중 4개(NCT05461820 0.39 · NCT04916990 0.39 ·
  NCT05944237 0.28 · NCT07447102 0.29)가 감점 대상이다. 스트레스셋 시험(T001 NCT05879653 · T003
  NCT07214727)은 아직 coverage_map에 없어 이 단계가 발동하지 않으므로, 이 문서가 든 T001/T003 반증
  사례 자체는 아직 열려 있다 — coverage_map을 trials_stress.json까지 확장하는 것이 남은 작업이다.
- **C를 2번(certainty보다 위)에 둔 것은 건희의 판단이다.** 같은 적격성 클래스라면 환자에게 치료 기회가
  되는 시험을 먼저 검토한다. certainty(4~6번)는 데모셋에서 비네트가 얇아서 생긴 아티팩트 성격이 크고
  (374개 기준 중 284개 UNKNOWN), 시험 레코드의 사실을 이기면 안 된다.
- **확신 없는 분류는 순위를 올리지 않는다.** 저확신 intent는 관찰과 같은 칸으로 내린다 — 추정 라벨이
  확인된 치료 시험을 밀어내는 일은 없다.
- **환자 부담은 아직 Phase 프록시뿐이다.** 방문 횟수·이동 거리·시험 기간이 레코드에 없다.
  향후 `trial_design` 사이드카로 붙인다.

### 측정된 영향 (고정 데모 트레이스 10명 × 4시험)

- 40개 순위 중 **24개 이동**, **1위가 바뀐 환자 5/10**, 최대 이동 폭 **2칸**.
  - S002 `NCT06963203`(관찰·추정, Phase NA) → `NCT05461820`(치료, PHASE4)
  - S003 `NCT04571658` → `NCT06607991`(치료, PHASE1)
  - S006 `NCT07339800` → `NCT05406037`
  - S007 `NCT06031025` → `NCT05535361`
  - S010 `NCT07228091` → `NCT06587945`(치료, PHASE4)
- **hard exclusion 10/10 유지** — INELIGIBLE 시험이 비-INELIGIBLE 위로 올라간 사례 0건
  (`ranking.hard_exclusion_holds`).

### 평가 독립성

`eval.py`는 `rank` 필드를 **읽지 않는다.** 순위 키를 바꿔도 블라인드 라벨과 짝지어진 정확도
**82.5%(n=40, unmatched 0)** 는 움직이지 않으며, `traces.json`은 디스크에서 손대지 않는다
(md5 핀 유지, 라벨 페어링 보존). 순위는 서빙 시점에 **메모리에서만** 재계산된다
(`api/trace.py`, `live_server.py`, `pipeline.recommend`).

순서 자체의 회귀는 별도로 막는다: `expected_ranking.json`이 골든 순서를 고정하고
`assert_traces.py` Check 7과 CI(`ranking.py --golden`)가 매 푸시마다 대조한다.
키를 의도적으로 바꾸면 골든 diff가 리뷰에 그대로 드러난다.
